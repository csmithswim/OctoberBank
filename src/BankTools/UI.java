package BankTools;

public interface UI {
    //Hacker challenge add min max options to the get number within a range
    public static int getNumber(String query);
    String getString(String query);
}
